# Source permission registry

The machine-readable registry is [`config/sources.yaml`](../config/sources.yaml). This file records the human review behind each entry.

## Matrix (reviewed 2026-09-24)

| Source | Method | Read permitted | Submit permitted | Basis | Rate limit (self-imposed) | Status |
|---|---|---|---|---|---|---|
| Greenhouse Job Board API | Public REST GET per employer board | ✅ | ❌ | Docs: "Job Board data is publicly available, so authentication is not required for any GET endpoints." Application POST requires the **employer's** Job Board API key (HTTP Basic). | 20/min | Implemented (read) |
| Lever Postings API | Public REST GET per employer site | ✅ | ❌ | Docs: published postings are publicly viewable and may be scraped by third parties. Apply POST requires a key generated by the **employer's** Super Admin; 2 req/s cap. | 20/min | Implemented (read) |
| Ashby Posting API | Public REST GET per job board | ✅ | ❌ | Public posting API for careers pages. **No submit endpoint**; candidates apply on Ashby-hosted pages. Docs do not explicitly address third-party aggregation, so re-check quarterly. | 20/min | Implemented (read) |
| LinkedIn | none | ❌ | ❌ | User Agreement prohibits scraping and automated access. Only the user's own data-export ZIP is accepted, as profile context. | n/a | Blocked |

### Consequence
No candidate-side submission route is currently permitted, so **the MVP routes every application through a handoff packet**. The submission gate, idempotency, limits, challenge handling and kill switch are implemented and tested against a test-only adapter. They take effect when a route is verified.

## Candidate sources to review next (NOT enabled)
| Source | Notes to verify before enabling |
|---|---|
| USAJOBS API | Free API key plus a User-Agent email header are required. Check the terms of use and rate limits. Applications go through USAJOBS/agency sites (no submit API). |
| SmartRecruiters Posting API | Public postings endpoint per company. Check the terms. |
| Workable / Recruitee / Personio public job widgets/APIs | Per-employer public feeds. Check the terms and caching rules for each. |
| Employer RSS/XML feeds | Only when the employer publishes them for syndication. |
| Aggregators (Adzuna, etc.) | Usually require an API key and attribution, and restrict use. Read the terms carefully; often non-commercial or display-only. |

## Review checklist (for each new source)
1. Link the **current** API docs and terms. Quote the sentence that permits the intended use (`read_basis`, `submit_basis`).
2. Note authentication, rate limits, caching/attribution rules and any geographic restrictions.
3. Confirm that automated candidate submission is **explicitly** permitted, not just technically possible. If it is not explicit, set `submit_permitted: false`.
4. Set `date_checked`. Connectors refuse to run 90 days later until re-reviewed.
5. Implement the connector (fetch + normalize only) with respx-mocked tests. Do not label it supported until tests pass.
6. Add a row to this matrix and an entry to `docs/OPS_LOG.md` when it is enabled in production.
