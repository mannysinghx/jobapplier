# Ops log

One line per action with a side effect outside the working tree: **timestamp · what · why · how to undo · verified?**

| Timestamp (local) | What | Why | Undo | Verified |
|---|---|---|---|---|
| 2026-09-24 19:25 | `git init -b main` in `/Users/manindersingh/-jobApplier` | Repo was empty and not under version control | `rm -rf .git` | yes |
| 2026-09-24 19:26 | Created `backend/.venv` (Python 3.12) and installed pip deps from PyPI | Build + test environment | `rm -rf backend/.venv` | yes (`pip list`) |
| 2026-09-24 19:43 | Tried to `initdb` a throwaway PG in the scratchpad with libpq's initdb | Test the migration on Postgres | none needed; failed before creating anything (no `postgres` binary next to that initdb) | yes |
| 2026-09-24 19:44 | Started a throwaway PostgreSQL 16 on **127.0.0.1:5499** (data dir in the session scratchpad, TCP only), created DBs `jobapplier_test`, `ja_pytest` | Verify the Alembic migration, audit triggers and the full test suite on Postgres. **Did not touch the user's existing Postgres on :5432.** | `pg_ctl -D <scratchpad>/pgtest stop` and delete the scratchpad dir | yes |
| 2026-09-24 19:45 | Two failed start attempts (Unix socket path >103 bytes; then "postmaster became multithreaded", fixed with `LC_ALL`) | Same as above | nothing to undo (server exited on its own) | yes (log read) |
| 2026-09-24 19:47 | Stopped the throwaway PostgreSQL on :5499 (`pg_ctl stop -m fast`) | Testing finished | n/a | yes (`lsof` shows nothing on :5499; :5432 still up) |
| 2026-09-24 ~19:55 | GET requests to public job-board APIs (Greenhouse `anthropic`, Lever `palantir`, Ashby `openai`) | Validate connectors against real payloads (read-only, permitted per registry, no personal data sent) | n/a (read-only) | yes |
| 2026-09-24 ~20:00 | Started demo API (uvicorn :8100) and Vite dev server (:5180) via the preview pane. Demo DB, key and data in the session scratchpad; synthetic profile only | Browser verification of UI + API | stop via the preview pane, or it ends with the session; delete the scratchpad `demo/` folder | yes (both ports were free beforehand) |
| 2026-09-24 ~20:05 | Toggled global pause ON then OFF in the **demo** DB only | Verify kill switch end to end (poll returned [] while paused) | n/a | yes |
| 2026-09-24 ~20:40 | Queried local Ollama (`/api/tags`, `/api/show`, `/api/chat` with synthetic facts) on 127.0.0.1:11434 | Choose a model; confirm the cloud-model flag; benchmark | n/a (read/inference only, local) | yes |
| 2026-09-24 ~20:50 | Demo DB only: backed up to `demo.before-0003.sqlite3`, then `ALTER TABLE packets ADD COLUMN generation` | Demo DB predates migration 0003 | restore the backup file | yes |
| 2026-09-24 ~20:55 | Enabled the local LLM (`qwen3.6:35b`) in the **demo** DB; generated letters for 5 synthetic applications | End-to-end check of the claim verifier on the real model | PUT /api/llm {enabled:false} | yes |
| 2026-09-24 ~21:05 | Research: web fetches of the LinkedIn/Indeed/ZipRecruiter/Dice/Ladders terms and developer pages (research agent + browser tab) | Permission review for the requested job sites | n/a (read-only) | yes |
| 2026-09-24 ~21:10 | Called Dice's public MCP server (`initialize`, `tools/list`, one `search_jobs` with 2 results) | Verify schema before building the connector (documented for custom MCP clients, no auth) | n/a (read-only) | yes |
| 2026-09-24 ~21:30 | Restarted the throwaway PostgreSQL 16 on 127.0.0.1:5499 (scratchpad), recreated `jobapplier_test`, ran migrations 0001-0004 + the suite, stopped it | Verify the new JSON-default migrations on Postgres | stop/delete the scratchpad dir | yes (stopped; :5432 untouched) |
| 2026-09-24 ~21:50 | Demo DB only: backed up to `demo.before-0004.sqlite3`, added `jobs.meta` and `source_boards.params`; added 2 Dice saved searches, polled Dice MCP (51 jobs), fetched details for 1 job; manual-added 1 synthetic job via the UI | Live verification of Dice + import UI | restore the backup file | yes |
| 2026-09-24 ~22:00 | Added remote `origin` = https://github.com/mannysinghx/jobapplier (PUBLIC, was empty) and pushed `main` | User requested push | delete/force-push the GitHub repo; `git remote remove origin` | see next check |
| 2026-09-24 ~22:15 | Railway project `jobapplier2026` (created by the user, 1 failed Railpack deploy): linked the local repo; added `Postgres` (template); added volume `jobapplier-volume` at `/data` on `jobapplier`; generated domain `jobapplier-production-53d3.up.railway.app`; set builder=DOCKERFILE, healthcheck `/api/health` (180s), 10 `JA_*` variables incl. a newly generated `JA_ENCRYPTION_KEY` (not printed anywhere) | User asked to deploy on Railway | delete the Postgres service/volume/domain in Railway; `railway environment edit` to revert vars; `railway unlink` | yes (variables + config read back) |
| 2026-09-24 ~22:20 | Pushed `8a51e44` to GitHub, which auto-deployed on Railway: deployment `4ad10d9e` SUCCESS. Verified live: health ok, UI + SPA routes 200, CSP/HSTS headers, /metrics 404, unauth API 401; via `railway ssh` (read-only): alembic at 0004, audit chain ok, /data mounted | Deploy | Railway dashboard → redeploy a previous deployment, or remove the service | yes |
